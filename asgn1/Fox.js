// fox.js

function drawFox() {
    const orange = [1.0, 0.5, 0.0, 1.0];
    const black = [0.1, 0.1, 0.1, 1.0];
  
    const triangles = [
      // Head
      [[-0.15, 0.4], [0.0, 0.65], [0.15, 0.4]], // 1
      // Left ear
      [[-0.09, 0.58], [-0.03, 0.67], [0.0, 0.58]], // 2
      // Right ear
      [[0.03, 0.67], [0.09, 0.58], [0.0, 0.58]], // 3
      // Nose
      [[-0.15, 0.4], [-0.07, 0.35], [0.0, 0.4]], // 4
      // Face inner triangle
      [[-0.06, 0.45], [0.0, 0.55], [0.06, 0.45]], // 5
  
      // Body
      [[-0.3, 0.0], [0.0, 0.4], [0.3, 0.0]], // 6
      // Chest inner triangle
      [[-0.15, 0.1], [0.0, 0.35], [0.15, 0.1]], // 7
  
      // Tail base
      [[0.15, 0.0], [0.4, 0.15], [0.2, 0.2]], // 8
      // Tail tip
      [[0.4, 0.15], [0.55, 0.0], [0.25, 0.0]], // 9
      // Tail inner tip
      [[0.35, 0.07], [0.45, 0.0], [0.3, 0.0]], // 10
  
      // Left foot
      [[-0.3, 0.0], [-0.27, -0.1], [-0.24, 0.0]], // 11
      // Right foot
      [[0.24, 0.0], [0.27, -0.1], [0.3, 0.0]], // 12
  
      // Chest inner detail
      [[-0.05, 0.18], [0.0, 0.3], [0.05, 0.18]], // 13
  
      // Left eye
      [[-0.05, 0.52], [-0.02, 0.54], [-0.02, 0.5]], // 14
      // Right eye
      [[0.02, 0.54], [0.05, 0.52], [0.02, 0.5]], // 15
  
      // Left ear inner
      [[-0.06, 0.64], [-0.03, 0.66], [-0.03, 0.61]], // 16
      // Right ear inner
      [[0.03, 0.66], [0.06, 0.64], [0.03, 0.61]], // 17
  
      // Neck fluff
      [[-0.1, 0.3], [0.0, 0.35], [-0.05, 0.2]], // 18
  
      // Tail fluff
      [[0.2, 0.1], [0.3, 0.15], [0.2, 0.2]], // 19
  
      // Nose tip
      [[-0.07, 0.35], [-0.04, 0.34], [-0.04, 0.36]], // 20
    ];
  
    for (let i = 0; i < triangles.length; i++) {
      let tri = new Triangle();
      tri.position = [...triangles[i][0], ...triangles[i][1], ...triangles[i][2]];
      tri.color = (i === 4 || i >= 10 || i === 20 || i === 14 || i === 15 || i === 16 || i === 17) ? black : orange;
      tri.size = 5;
      g_shapesList.push(tri);
    }
  
    renderAllShapes();
  }
  